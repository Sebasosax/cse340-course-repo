<%- include('partials/header') %>

<main>
    <h1><%= title %></h1>

    <p>Here you will find our service project categories.</p>

    <% categories.forEach(category => { %>
        <div>
            <h2><%= category.name %></h2>
        </div>
    <% }) %>

</main>

<%- include('partials/footer') %>